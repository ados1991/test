
import argparse

parser = argparse.ArgumentParser(description="Example: filter by completion status")

parser.add_argument(
    "--status",
    choices=["complete", "nocomplete", "all"],
    default="all",
    help="Filter results by status: 'complete', 'nocomplete', or 'all' (default: all)"
)

args = parser.parse_args()
